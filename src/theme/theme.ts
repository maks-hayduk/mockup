interface ITheme {
  blackColor: string;
  buttonActiveColor: string;
  buttonColor: string;
  headerColor: string;
}

const theme: ITheme = {
  blackColor: 'black',
  buttonActiveColor: '#d6d6d6',
  buttonColor: '#e5e5e5',
  headerColor: '#e8e7e7'
};

export default theme;
